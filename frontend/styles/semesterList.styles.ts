import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        marginVertical: 16,
        paddingHorizontal: 16,
    },
    semesterTitle: {
        fontSize: 22,
        fontWeight: '700',
        color: '#333',
        marginBottom: 12,
    }
});
