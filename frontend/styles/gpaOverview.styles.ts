import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
    container: {
        backgroundColor: '#007AFF',
        padding: 16,
        borderRadius: 12,
        marginHorizontal: 16,
        marginVertical: 12,
        alignItems: 'center',
    },
    gpaTitle: {
        fontSize: 18,
        color: '#FFFFFF',
        fontWeight: '600',
        marginBottom: 4,
    },
    gpaValue: {
        fontSize: 32,
        color: '#FFFFFF',
        fontWeight: 'bold',
    }
});
